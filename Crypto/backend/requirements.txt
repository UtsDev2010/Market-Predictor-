# =============================================================================
# Market Predictor — Python Dependencies
# Pin exact versions for reproducible production builds.
# =============================================================================

# --- Core framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0

# --- Settings & validation ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Database ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0           # async PostgreSQL driver
psycopg2-binary==2.9.9    # sync driver for Alembic
alembic==1.13.1

# --- HTTP clients ---
httpx==0.27.0
aiohttp==3.9.5
tenacity==8.3.0           # retry logic

# --- Scheduling ---
apscheduler==3.10.4

# --- Telegram ---
python-telegram-bot==21.2

# --- Data & ML ---
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
torch==2.3.0              # LSTM model (CPU build; swap for +cu121 on GPU hosts)

# --- Logging ---
python-json-logger==2.0.7

# --- Utilities ---
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4    # password hashing
python-jose[cryptography]==3.3.0  # JWT

# --- Dev / test ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0             # async test client
faker==25.0.0
