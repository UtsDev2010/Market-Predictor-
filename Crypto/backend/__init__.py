"""Market Predictor backend package root.

Declaring this package makes the absolute imports used across the codebase
(`from backend.config...`, `from backend.database...`, etc.) resolve cleanly
when the project root is on PYTHONPATH.
"""

__version__ = "1.0.0"
